<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External storage (camera capture fallback) -->
    <external-files-path
        name="external_pictures"
        path="Pictures/" />

    <!-- Cache directory support -->
    <cache-path
        name="cache_images"
        path="." />
</paths>